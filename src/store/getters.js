export const token = state => state.token

export const collections = state => state.collections

export const circles = state => state.circles

export const questions = state => state.questions

export const attentions = state => state.attentions

export const answers = state => state.answers

export const followings = state => state.followings

export const followers = state => state.followers

export const likes = state => state.likes

export const tags = state => state.tags
